{
  "github": {
    "release": true,
    "releaseName": "v${version}",
    "assets": "E:/ircontrol/out/make/squirrel.windows/x64/*"
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "after:bump": ["npm run make"],
    "after:release": "echo Successfully released ${name} v${version} to ${repo.repository}."
  }
}
